<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.warehouse.mapper.BrandMapper">
    <!--
      //查询所有品牌的方法
      public List<Brand> findAllBrand()
    -->
    <select id="findAllBrand" resultType="com.warehouse.entity.Brand">
        select *
        from brand
    </select>

    <select id="selectBrandCount" resultType="int">
        select count(*)
        from brand
        <where>
            <if test="brandName != null and brandName != ''">
                and brand_name like concat('%', #{brandName}, '%')
            </if>
            <if test="brandInitial != null and brandInitial != ''">
                and brand_initial like concat('%', #{brandInitial}, '%')
            </if>
            <if test="brandDesc != null and brandDesc != ''">
                and brand_desc like concat('%', #{brandDesc}, '%')
            </if>
        </where>
    </select>

    <select id="selectBrandPage" resultType="com.warehouse.entity.Brand">
        select *
        from brand
        <where>
            <if test="brand.brandName != null and brand.brandName != ''">
                and brand_name like concat('%', #{brand.brandName}, '%')
            </if>
            <if test="brand.brandInitial != null and brand.brandInitial != ''">
                and brand_initial like concat('%', #{brand.brandInitial}, '%')
            </if>
            <if test="brand.brandDesc != null and brand.brandDesc != ''">
                and brand_desc like concat('%', #{brand.brandDesc}, '%')
            </if>
        </where>
        limit #{page.limitIndex}, #{page.pageSize}
    </select>

    <insert id="saveBrand">
        insert into brand(brand_name, brand_initial, brand_desc)
        values(#{brandName}, #{brandInitial}, #{brandDesc})
    </insert>

    <update id="updateBrand">
        update brand
        set brand_name = #{brandName},
            brand_initial = #{brandInitial},
            brand_desc = #{brandDesc}
        where brand_id = #{brandId}
    </update>

    <delete id="deleteBrand">
        delete from brand
        where brand_id = #{brandId}
    </delete>
</mapper>